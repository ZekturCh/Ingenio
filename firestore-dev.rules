rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Solo para desarrollo inicial.
    // Cambia a firestore.rules antes de poner datos reales.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
